# Base Workspace

This is the bundled default workspace template for BloomAgent.

It is intentionally small and probe-agnostic. Probe-specific conditioning
scripts add the context needed for a given behavioral evaluation.
